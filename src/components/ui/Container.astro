---
interface Props {
  class?: string;
}

const { class: className } = Astro.props;
---

<div class:list={['mx-auto w-full max-w-[28rem] px-5 md:max-w-[42rem] lg:max-w-[64rem]', className]}>
  <slot />
</div>
